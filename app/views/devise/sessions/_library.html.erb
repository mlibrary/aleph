<div class="row">
  <div class="col-md-9">
    <p>Danish public libraries and danish research libraries can login
    to DTU Library printed collection with library number and a
    self-chosen password. If your library does not have a password for
    DTU Library Login, please create a new password using the link
    below the login form.</p>
  </div>
</div>
<%= form_for(resource, :as => resource_name,
      :url => session_path(resource_name)) do |f| %>
  <div class="row">
    <div class="col-md-3">
      <%= f.label :library_id %>
      <%= f.text_field :library_id, :autofocus => true, :class => 'input-block-level' %>
    </div>
    <div class="col-md-3">
      <%= f.label :password %>
      <%= f.password_field :password, :class => 'input-block-level' %>
    </div>
    <%= hidden_field_tag 'lt', session['lt'].ticket %>
    <div class="col-md-3">
      <label>&nbsp;</label>
      <%= f.submit I18n.t('riyosha.sign_in.submit'), :class => 'btn' %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-offset-3 col-md-3">
  <% if devise_mapping.rememberable? -%>
      <label class="checkbox">
        <%= f.check_box :remember_me %> <%= f.label :remember_me %>
      </label>
  <% end -%>
    </div>
    <div class="col-md-6">
  <%- if devise_mapping.recoverable? %>
    <%= link_to 'Create new password', new_password_path(resource_name) %>
  <% end -%>
    </div>
  </div>
<% end %>
